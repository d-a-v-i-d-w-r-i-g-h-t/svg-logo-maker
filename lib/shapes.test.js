const { Triangle } = require('./shapes.js');

// A testing suite for Shapes is created.
describe('Shapes', () => {

  // A test is created to check that the text matches what has been rendered.
  describe('shape color', () => {
    it('should return a shape with color blue', () => {

      // Arrange
      const color = "blue";

      // Act
      const shape = new Triangle( color );
      // Assert
      expect(shape.shapeColor).toEqual(color);

    });
  });

  // A test is created to check that the color matches what has been rendered.
  describe('shape color', () => {
    it('should return a shape with color blue', () => {

      // Arrange
      const color = "blue";

      // Act
      const shape = new Triangle( color );
      // Assert
      expect(shape.shapeColor).toEqual(color);

    });
  });

  // A test is created to check that the color matches what has been rendered.
  describe('shape color', () => {
    it('should return a shape with color blue', () => {

      // Arrange
      const color = "blue";

      // Act
      const shape = new Triangle( color );
      // Assert
      expect(shape.shapeColor).toEqual(color);

    });
  });

  // A test is created to check that the color matches what has been rendered.
  describe('shape color', () => {
    it('should return a shape with color blue', () => {

      // Arrange
      const color = "blue";

      // Act
      const shape = new Triangle( color );
      // Assert
      expect(shape.shapeColor).toEqual(color);

    });
  });

  
});

